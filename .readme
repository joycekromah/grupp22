Körinstruktioner för IntelliJ


STEG 1
------------------------------------------
Skapa en .env fil i rotmappen och lägg in de bifogade API-nycklarna.


STEG 2
------------------------------------------
Kopiera eller skriv in detta i terminalen:
python -m venv venv
Mac o linux:
source venv/bin/activate

Windows:
venv\Scripts\activate


eller:
 
python3 -m venv venv
Mac o linux:
source venv/bin/activate

Windows:
venv\Scripts\activate


STEG 3
------------------------------------------
Kopiera eller skriv in detta i terminalen:
pip3 install -r requirements.txt


STEG 4
------------------------------------------
Kopiera eller skriv in detta i terminalen om du har macOS eller Linux:
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.40.1/install.sh | bash && . "$HOME/.nvm/nvm.sh" && nvm install 22 && node -v && nvm current && npm -v

Kör man windows ska man istället skriva:
winget install Schniz.fnm && fnm install 22

STEG 5
------------------------------------------
Kopiera eller skriv in detta i terminalen:
uvicorn main:app --host 127.0.0.1 --port 8000 --reload


STEG 6
------------------------------------------
Gå in på filen "index.html" i IntelliJ. Klicka på "Run"-knappen.


STEG 7
------------------------------------------
Skriv in ett sökord och välj en eller alla datakällor som du önskar analysera sentiment från. 
Tryck på “Search”. Detta kan ta några minuter.